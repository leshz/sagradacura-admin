export type config = {
  token: string;
  active: boolean;
  default_currency: string;
  back_urls: string;
  webhook_pass: string;
  bussiness_description: string;
  notification_url: string;
  send_emails: boolean;
  email: string;
};
